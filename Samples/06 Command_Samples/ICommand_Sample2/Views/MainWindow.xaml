<Window x:Class="ICommand_Sample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ICommand_Sample.Views"
        xmlns:vm="clr-namespace:ICommand_Sample.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="225" WindowStartupLocation="CenterScreen" FontSize="16">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Button Content="{Binding ButtonContent}" Width="65" Height="65" Command="{Binding ClickCommand}"/>

        <StackPanel Grid.Row="1">
            <TextBox Name="UpTextBox" Margin="5,5" Text="{Binding TextItem.UpText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Move" Margin="5,0" Command="{Binding MoveCommand }" />
            <TextBox Name="DownTextBox" Margin="5,5" Text="{Binding TextItem.DownText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        
    </Grid>
</Window>
