<Application x:Class="Resources_Sample.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Resources_Sample"
             StartupUri="MainWindow.xaml">

    <!-- Anwendungsweite Ressourcen 
         - Key-Value Liste (Hashtable), in die beliebige Objekte abgelegt werden können (in unserem Fall Elemente des Typs 'Color') 
    -->
    <Application.Resources>
        <!--
        <Color x:Key="Farbe1">#ff0000</Color>
        <SolidColorBrush x:Key="Füllung1" Color="{DynamicResource Farbe1}" />
        <Color x:Key="Farbe2">#00ff00</Color>
        <SolidColorBrush x:Key="Füllung2" Color="{DynamicResource Farbe2}" />
        --> 
        
        <!-- 
            Resources ausgelagert in ein externes xaml-File (welches ein sog. ResourceDictionary beinhaltet).
            Nutzen? Wiederverwendbarkeit der Resourcen auch für andere Projekte 
        -->
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ResourcesDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
        
    </Application.Resources>
</Application>
